modButtonsServer = function(id, mas) {
  moduleServer(id, function(input, output, session) {
      observe(intpu$btn, {
          browser()
      })
    #   output$val <- renderText({
    # paste("Value:", input$btn)

  })
}
